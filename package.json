{
  "name": "sudoku",
  "version": "0.0.1",
  "description": "This is a demonstration repository",
  "main": "index.js",
  "scripts": {
    "coverage": "babel-node node_modules/.bin/babel-istanbul cover _mocha -- --timeout 30000 test/**/*.spec.js",
    "test": "mocha --compilers js:babel-core/register test/**/*.spec.js",
    "watch": "mocha --watch --compilers js:babel-core/register test/**/*.spec.js"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-core": "^6.7.7",
    "babel-istanbul": "^0.8.0",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "lodash": "^4.12.0"
  }
}
